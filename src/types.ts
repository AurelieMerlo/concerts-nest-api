export type Result = {
    band: string,
    location: string,
    date: number,
    longitude: number,
    latitude: number,
  }
  
  export type Coordinates = {
    longitude: string,
    latitude: string,
    radius: string
  }